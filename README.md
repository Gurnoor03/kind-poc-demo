# kind — PoC Demo Fork

This fork demonstrates the mutable branch reference vulnerability in Kubernetes Prow CI.
The `ci-audit-logging` branch mirrors the structure of `ii/kind/ci-audit-logging`.

**For bug bounty PoC only — not for production use.**
