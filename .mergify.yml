  queue_rules:
    - name: default
      queue_conditions:
        - "check-success=ci-passes"
      merge_conditions:
        - "approved-reviews-by>=1"

  pull_request_rules: []

  merge_protections_settings:
    reporting_method: check-runs
